### **Miira-Crypt** is a program for encrypting files using the AES-256-CBC cryptographic method. ###
Everything runs in a simple graphical interface written in Rust, which means it takes up little disk space and doesn’t strain the RAM.
Combines the Rust and C++ programming languages.
